# Secrets — never commit
.env
.env.local
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Database + generated artifacts (regenerable, and large)
backend/data/*.db
backend/data/*.db-journal
backend/data/*.db-wal
backend/data/*.db-shm
backend/data/artifacts/

# Node
node_modules/
frontend/dist/

# OS / tooling
.DS_Store
.code-review-graph/
.vscode/

# The assignment brief is Beast Life's document, not ours — excluded so a
# public repo does not republish their hiring material.
Beast_Life_AI_Engineering_Assignment.pdf

# Stray generated previews at the repo root
/image_*.png
/*.mp4
/*.mov

# Demo recording stills — sent with the submission email, not committed
.demo_shots/

# Assets downloaded by verify_campaign.py --api
.verify_downloads/

# Personal build notes and interview prep — not part of the submission
NOTES.md
INTERVIEW_PREP.md
DEMO_SCRIPT.md
